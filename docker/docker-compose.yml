version: "3.8"
services:
  control_api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - ../config:/app/config
      - ../logs:/app/logs
